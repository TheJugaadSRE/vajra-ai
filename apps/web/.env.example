# Local dev default. For a Vercel deployment, set this in the Vercel project's
# environment variables instead (see README.md's "Deploying a public demo"
# section) — CRA bakes REACT_APP_* vars in at build time, so a .env file here
# has no effect on what Vercel builds.
REACT_APP_API_BASE=http://localhost:4000

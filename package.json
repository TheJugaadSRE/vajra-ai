{
  "name": "vajra-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "services/api-local",
    "infra/cdk",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build -w packages/core",
    "dev": "concurrently -k -n core,api,web -c blue,green,magenta \"npm run build -w packages/core -- --watch\" \"npm run dev -w services/api-local\" \"npm run start -w apps/web\"",
    "test": "npm run build -w packages/core && npm run test -w packages/core && npm run test -w services/api-local && npm run typecheck -w infra/cdk",
    "cdk:synth": "npm run synth -w infra/cdk"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
